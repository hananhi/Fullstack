/*
 const people = ["Greg", "Mary", "Devon", "James"]

people.shift(); / people.splice(0,1);
people.pop();
people.splice(0,0,'matt');
people.push('hanan');
people.slice(2);

let position = people.indexOf('mary');

let position = people.indexOf('foo');
it return -1  The reason behind this choice is that 
-1 is a memorable number that cannot be mistaken for an actual index in the array

--

withBob=people.slice() ;
withBob.push('Bob');

*/